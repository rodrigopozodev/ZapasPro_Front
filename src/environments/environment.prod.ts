// Archivo de configuración del entorno para la aplicación Angular
export const environment = {
  production: false, // Indica si la aplicación está en modo producción
  apiUrl: 'http://localhost:3000/api' // URL base para la API del backend
};
